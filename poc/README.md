# Degree Pathways — POC

Original proof-of-concept: static JSON data + simple HTML/CSS/JS UI. Kept for reference. The real app uses `../api` and (planned) `../web`.

## Run POC locally

```bash
cd poc
npm install
node server.js
```

Open http://localhost:3001
